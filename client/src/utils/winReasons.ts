/** Neutral labels for game_results.win_reason values, shared by profile/match views. */
export const WIN_REASON_LABEL: Record<string, string> = {
  tower_destroyed: 'Tower destroyed',
  tower_built:     'Tower built',
  resources:       'Resources',
  timeout:         'Timeout',
  afk:             'AFK',
  forfeit:         'Forfeit',
}
