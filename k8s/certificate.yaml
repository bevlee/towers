apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: bevsoft-wildcard
  namespace: towers
spec:
  secretName: bevsoft-wildcard-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  commonName: '*.bevsoft.com'
  dnsNames:
    - '*.bevsoft.com'
    - bevsoft.com
