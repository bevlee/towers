apiVersion: v1
kind: Secret
metadata:
  name: pocketbase-admin
  namespace: towers
type: Opaque
# stringData accepts plain text; kubectl will base64-encode it for storage.
# Replace these values before applying.
stringData:
  email: "admin@example.com"
  password: "changeme-use-a-strong-password"
