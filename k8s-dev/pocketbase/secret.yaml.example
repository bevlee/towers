apiVersion: v1
kind: Secret
metadata:
  name: pocketbase-admin
  namespace: towers-dev
type: Opaque
# stringData accepts plain text; kubectl will base64-encode it for storage.
# Replace these values before applying. Use different credentials from prod.
stringData:
  email: "admin@example.com"
  password: "changeme-use-a-strong-password"
